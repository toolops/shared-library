name: Publish shared library

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '.github/**'
    branches:
      - master

jobs:
  publish:
    uses: ToolOps/reusable-workflows/.github/reusable_workflows/helm-release-workflow.yml@v0.0.1
    with:
      chart_name: shared-library
    secrets: inherit